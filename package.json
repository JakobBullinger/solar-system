{
  "name": "solar-system",
  "private": true,
  "description": "A live orrery — interactive solar system with real orbital mechanics",
  "scripts": {
    "build": "node build.js",
    "dev": "node serve.js",
    "test": "node test/run.js",
    "e2e": "playwright test --config tools/e2e/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
